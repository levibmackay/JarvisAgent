"""Meeting capture, transcription, and summarization.

System audio requires the BlackHole loopback driver
(`brew install blackhole-2ch`) plus a macOS Multi-Output Device so you
still hear the call; without it, recording falls back to the microphone.
"""
